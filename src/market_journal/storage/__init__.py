"""Persistence for decisions, reports, and strategy memory.

Artifacts (committed back by CI so the feedback loop persists):
    data/decisions/YYYY-MM-DD.json   — full decision record
    data/reports/YYYY-MM-DD.md       — human-readable report
    data/memory/strategy_memory.json — conservative learning state
"""
